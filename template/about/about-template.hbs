{{#each data}}
  <h1>{{name}}</h1>
  <img class="float-start me-3 rounded-4" src="{{thumbpath}}" alt="about" />
  <div class="text-black-50 mb-4">{{{summary}}}</div>
  <a href="?request=aboutus/{{id}}" id="readmore">Read more</a>
{{/each}}